<%= form_with(model: rate, url: admin_path, local: true) do |form| %>
  <% if rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rate.errors.count, "error") %> prohibited this rate from being saved:</h2>

      <ul>
      <% rate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mantissa %>
    <%= form.number_field :mantissa %>
    <span>.</span>
    <%= form.number_field :fraction %>
  </div>

  <div class="field">
    <%= form.label :overwrite %>
    <%= form.text_field :overwrite %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
